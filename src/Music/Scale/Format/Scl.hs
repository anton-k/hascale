module Music.Scale.Format.Scl(
    Scl(..)
  , fromScale
  , toText
) where

import Data.Text

import Music.Scale.Format.Types

data Scl = Scl

fromScale :: Scale -> Scl
fromScale = undefined

toText :: Scl -> Text
toText = undefined
